<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorAccent"
    tools:context="se.exjobb.feedloopt.fragments.LoginFragment"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin">


    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textEmailAddress"
        android:ems="10"
        android:background="@color/colorPrimary"
        android:layout_marginTop="100dp"
        android:padding="10dp"
        android:id="@+id/login_email_field"
        android:layout_below="@+id/login_feedloop_headline"
        android:layout_alignParentEnd="true"
        android:hint="Email"
        android:textSize="12sp" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:padding="10dp"
        android:textSize="12sp"
        android:background="@color/colorPrimary"
        android:id="@+id/login_password_field"
        android:hint="Password"
        android:singleLine="false"
        android:selectAllOnFocus="false"
        android:layout_below="@+id/login_email_field"
        android:layout_alignParentStart="true"
        android:layout_marginTop="8dp" />

    <Button
        android:text="Login"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="16dp"
        android:padding="10dp"
        android:id="@+id/login_button"
        android:layout_below="@+id/login_password_field"
        android:background="#FD6776"
        android:textColor="#fff"
        android:layout_centerHorizontal="true"
        android:textSize="14sp" />

    <Button
        android:text="Register"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:padding="10dp"
        android:layout_below="@+id/login_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:background="#0076FF"
        android:textColor="#fff"
        android:id="@+id/login_register_button" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/login_feedloop_headline"
        android:text="Feedloop Teacher"
        android:textSize="28dp"
        android:layout_marginTop="75dp"

        android:textColor="#ffffff"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
